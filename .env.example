# this is an example .env file, use it to create your own .env file and place it in the root of the project

# database / queue infrastructure
# KH_DATABASE supports sqlite (default) or PostgreSQL, e.g.:
# KH_DATABASE=postgresql+psycopg2://user:password@host:5432/maia
KH_DATABASE=
# optional future queue backend endpoint (reserved for Redis queue workers)
MAIA_REDIS_URL=

# API runtime bind settings
MAIA_SERVER_NAME=0.0.0.0
MAIA_SERVER_PORT=8000
# Assemble-and-run SSE hard timeout in seconds.
# Set to 0 to disable the hard cutoff (recommended for long Brain runs).
MAIA_ASSEMBLE_RUN_MAX_SECONDS=0

# settings for the primary OpenAI-compatible chat runtime
# Configure these explicitly for your provider. Maia no longer falls back to
# api.openai.com or gpt-* defaults automatically.
MAIA_LLM_API_BASE=
MAIA_LLM_API_KEY=
MAIA_LLM_CHAT_MODEL=
MAIA_LLM_EMBEDDINGS_MODEL=
MAIA_LLM_CHAT_MODEL_FALLBACKS=
# Legacy aliases:
# OPENAI_API_BASE=
# OPENAI_API_KEY=
# OPENAI_CHAT_MODEL=
# OPENAI_EMBEDDINGS_MODEL=
MAIA_FAST_QA_TEMPERATURE=0.2
MAIA_FAST_QA_SOURCE_SCAN=120
MAIA_FAST_QA_MAX_SOURCES=18
MAIA_FAST_QA_MAX_CHUNKS_PER_SOURCE=3

# Agent LLM Intelligence (set to 0 to disable individual layers)
MAIA_AGENT_LLM_PLANNER_ENABLED=1
MAIA_AGENT_LLM_PLAN_CRITIC_ENABLED=1
MAIA_AGENT_LLM_QUERY_REWRITE_ENABLED=1
MAIA_AGENT_LLM_INTENT_ENABLED=1
MAIA_AGENT_LLM_INTENT_TAGS_ENABLED=1
MAIA_AGENT_LLM_VERIFICATION_ENABLED=1
MAIA_AGENT_LLM_RECOVERY_ENABLED=1
MAIA_AGENT_LLM_EMAIL_POLISH_ENABLED=1
MAIA_AGENT_LLM_CONTACT_POLISH_ENABLED=1
MAIA_AGENT_LLM_RESPONSE_POLISH_ENABLED=1
MAIA_AGENT_LLM_PERSONALIZATION_ENABLED=1
MAIA_AGENT_LLM_CONTEXT_SUMMARY_ENABLED=1
MAIA_AGENT_LLM_STEP_SUMMARY_ENABLED=1
MAIA_AGENT_LLM_TASK_REWRITE_ENABLED=1
MAIA_AGENT_LLM_LOCATION_BRIEF_ENABLED=1
MAIA_AGENT_LLM_TASK_CONTRACT_ENABLED=1
MAIA_AGENT_LLM_DELIVERY_CHECK_ENABLED=1
MAIA_AGENT_LLM_FACT_PROBE_ENABLED=1
# LLM reliability tuning
MAIA_AGENT_LLM_RETRIES=2
MAIA_AGENT_LLM_RETRY_BACKOFF_MS=250
# Qwen/DashScope thinking control (default off to reduce token burn).
MAIA_AGENT_LLM_ENABLE_THINKING_DEFAULT=false
# Whether to include fallback model list for generic calls by default.
MAIA_AGENT_LLM_USE_FALLBACK_MODELS=true
# Comma-separated fallback model list used when the primary model fails
OPENAI_CHAT_MODEL_FALLBACKS=

# settings for Azure OpenAI
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
OPENAI_API_VERSION=2024-02-15-preview
AZURE_OPENAI_CHAT_DEPLOYMENT=
AZURE_OPENAI_EMBEDDINGS_DEPLOYMENT=

# settings for Cohere
COHERE_API_KEY=<COHERE_API_KEY>

# settings for Mistral
# MISTRAL_API_KEY=placeholder

# settings for VoyageAI
VOYAGE_API_KEY=<VOYAGE_API_KEY>

# settings for Jina web reader / search
JINA_API_KEY=
JINA_URL=https://r.jina.ai/

# settings for web URL crawling
# 0 means unlimited
WEB_READER_MAX_DEPTH=0
WEB_READER_MAX_PAGES=0
WEB_READER_SAME_DOMAIN_ONLY=true
WEB_READER_TIMEOUT=20

# async ingestion jobs (for large batch PDF/URL indexing)
MAIA_INGEST_WORKERS=1
MAIA_INGEST_FILE_BATCH_SIZE=25
MAIA_INGEST_URL_BATCH_SIZE=25
MAIA_INGEST_WORKDIR=.maia_ingestion_jobs
MAIA_INGEST_KEEP_WORKDIR=false

# upload/index reader routing
MAIA_UPLOAD_INDEX_READER_MODE=default
MAIA_UPLOAD_INDEX_QUICK_MODE=true
MAIA_UPLOAD_PADDLEOCR_ENABLED=true
MAIA_UPLOAD_PADDLEOCR_LANG=en
MAIA_UPLOAD_PADDLEOCR_USE_GPU=false
MAIA_UPLOAD_PADDLEOCR_RENDER_DPI=220
# 0 means all pages
MAIA_UPLOAD_PADDLEOCR_MAX_PAGES=0
# Optional remote PaddleOCR-VL API mode for heavy PDFs.
# When enabled, heavy PDF extraction uses the HTTP API instead of local paddleocr runtime.
MAIA_UPLOAD_PADDLEOCR_VL_API_ENABLED=true
MAIA_UPLOAD_PADDLEOCR_VL_API_URL=
MAIA_UPLOAD_PADDLEOCR_VL_API_TOKEN=
MAIA_UPLOAD_PADDLEOCR_VL_API_TIMEOUT_SECONDS=120
# PDF=0, image=1
MAIA_UPLOAD_PADDLEOCR_VL_API_FILE_TYPE=0
MAIA_UPLOAD_PADDLEOCR_VL_API_USE_DOC_ORIENTATION_CLASSIFY=false
MAIA_UPLOAD_PADDLEOCR_VL_API_USE_DOC_UNWARPING=false
MAIA_UPLOAD_PADDLEOCR_VL_API_USE_CHART_RECOGNITION=false
# startup dependency checks for PaddleOCR heavy-PDF routing
MAIA_UPLOAD_PADDLEOCR_STARTUP_CHECK=true
# true = fail startup if PaddleOCR/PyMuPDF dependency checks fail
MAIA_UPLOAD_PADDLEOCR_STARTUP_STRICT=false
# true = pre-initialize PaddleOCR engine during startup
MAIA_UPLOAD_PADDLEOCR_STARTUP_WARMUP=false
# VLM reviewer/upgrader for classifier (never downgrades heavy -> normal)
MAIA_UPLOAD_PDF_VLM_REVIEW_ENABLED=false
MAIA_UPLOAD_PDF_VLM_REVIEW_MODEL=qwen2.5vl:7b
# defaults to OLLAMA_BASE_URL when empty
MAIA_UPLOAD_PDF_VLM_BASE_URL=
MAIA_UPLOAD_PDF_VLM_REVIEW_TIMEOUT_SECONDS=20
MAIA_UPLOAD_PDF_VLM_REVIEW_RENDER_DPI=180
# 0 means no cap (check all selected pages)
MAIA_UPLOAD_PDF_VLM_REVIEW_MAX_PAGES=0
# VLM extraction merge on heavy PDF pages
MAIA_UPLOAD_PDF_VLM_EXTRACT_ENABLED=false
MAIA_UPLOAD_PDF_VLM_EXTRACT_MODEL=qwen2.5vl:7b
MAIA_UPLOAD_PDF_VLM_EXTRACT_TIMEOUT_SECONDS=45
MAIA_UPLOAD_PDF_VLM_EXTRACT_RENDER_DPI=220
# 0 means no cap
MAIA_UPLOAD_PDF_VLM_EXTRACT_MAX_PAGES=0
# startup checks for Ollama VLM runtime/model availability
MAIA_UPLOAD_PDF_VLM_STARTUP_CHECK=true
MAIA_UPLOAD_PDF_VLM_STARTUP_STRICT=false

# settings for local models
LOCAL_MODEL=qwen2.5:7b
LOCAL_MODEL_EMBEDDINGS=nomic-embed-text
OLLAMA_BASE_URL=http://127.0.0.1:11434

# settings for GraphRAG
GRAPHRAG_API_KEY=
GRAPHRAG_LLM_MODEL=
GRAPHRAG_EMBEDDING_MODEL=

# set to true if you want to use customized GraphRAG config file
USE_CUSTOMIZED_GRAPHRAG_SETTING=false

# settings for Azure DI
AZURE_DI_ENDPOINT=
AZURE_DI_CREDENTIAL=

# settings for Adobe API
# get free credential at https://acrobatservices.adobe.com/dc-integration-creation-app-cdn/main.html?api=pdf-extract-api
# also install pip install "pdfservices-sdk@git+https://github.com/niallcm/pdfservices-python-sdk.git@bump-and-unfreeze-requirements"
PDF_SERVICES_CLIENT_ID=
PDF_SERVICES_CLIENT_SECRET=

# settings for PDF.js
PDFJS_VERSION_DIST="pdfjs-4.0.379-dist"

# Maia Company Agent integrations (optional env fallback)
# Prefer storing per-tenant credentials via /api/agent/connectors/credentials.

# Slack
SLACK_BOT_TOKEN=

# Google Ads
GOOGLE_ADS_DEVELOPER_TOKEN=
GOOGLE_ADS_CUSTOMER_ID=
GOOGLE_ADS_LOGIN_CUSTOMER_ID=
GOOGLE_ADS_ACCESS_TOKEN=
GOOGLE_ADS_REFRESH_TOKEN=

# Google Workspace
GOOGLE_WORKSPACE_ACCESS_TOKEN=
GOOGLE_WORKSPACE_REFRESH_TOKEN=

# Microsoft 365
M365_ACCESS_TOKEN=

# Google OAuth 2.0
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8000/api/agent/oauth/google/callback

# Bing Web Search (Azure)
AZURE_BING_API_KEY=
BING_SEARCH_ENDPOINT=https://api.bing.microsoft.com/v7.0/search

# Google Maps (Places / Geocoding / Distance Matrix)
GOOGLE_MAPS_API_KEY=

# Brave Search
BRAVE_SEARCH_API_KEY=

# Gmail API
# Service account DWD (recommended for server-side report sending, no interactive OAuth)
# Preferred: MAIA_GMAIL_SA_JSON_B64 (base64 JSON from secret manager)
# Fallback: MAIA_GMAIL_SA_JSON_PATH (absolute path)
# Never commit raw service-account JSON or private keys.
MAIA_GMAIL_SA_JSON_PATH=
MAIA_GMAIL_SA_JSON_B64=
MAIA_GMAIL_IMPERSONATE=disan@micrurus.com
MAIA_GMAIL_FROM=disan@micrurus.com

# Legacy OAuth token flow (optional)
GMAIL_ACCESS_TOKEN=
GMAIL_REFRESH_TOKEN=
AGENT_GMAIL_PLAYWRIGHT_HEADLESS=true
AGENT_GMAIL_PLAYWRIGHT_SLOW_MO_MS=50
AGENT_GMAIL_PLAYWRIGHT_PROFILE_DIR=.maia_agent/playwright/gmail_profile

# Google Calendar API
GOOGLE_CALENDAR_ACCESS_TOKEN=
GOOGLE_CALENDAR_REFRESH_TOKEN=

# Google Analytics Data API (GA4)
GOOGLE_ANALYTICS_ACCESS_TOKEN=
GOOGLE_ANALYTICS_REFRESH_TOKEN=
GOOGLE_ANALYTICS_PROPERTY_ID=

# Invoice providers
QUICKBOOKS_ACCESS_TOKEN=
QUICKBOOKS_REALM_ID=
XERO_ACCESS_TOKEN=
XERO_TENANT_ID=

# Email verification
EMAIL_VALIDATION_PROVIDER=abstractapi
EMAIL_VALIDATION_API_KEY=

# SMTP
AGENT_SMTP_HOST=
AGENT_SMTP_PORT=587
AGENT_SMTP_USERNAME=
AGENT_SMTP_PASSWORD=

# variable for authentication method selection
# for authentication with google leave empty
# for authentication with keycloak :
# AUTHENTICATION_METHOD="KEYCLOAK"

AUTHENTICATION_METHOD=

# API identity mode
# - false: allow dev fallback user when X-User-Id/user_id is missing
# - true: require explicit user identity for every request
MAIA_REQUIRE_EXPLICIT_USER_ID=false
MAIA_DEV_DEFAULT_USER_ID=default

# settings for keycloak
KEYCLOAK_SERVER_URL=
KEYCLOAK_CLIENT_ID=
KEYCLOAK_REALM=
KEYCLOAK_CLIENT_SECRET=
